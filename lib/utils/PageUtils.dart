import 'package:flutter/material.dart';

class PageUtils {
  static double bodyPadding = 12.0;
  static const List<IconData> fabIcons = const [Icons.done, Icons.close];
  static const List<Color> fabIconsColors = const [
    Color(0xFF166D97),
    Colors.red
  ];
}
